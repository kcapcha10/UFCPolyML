# Scrapy project configuration. Points the CLI at the settings module; the spider
# modules themselves are declared via SPIDER_MODULES inside that settings module.
[settings]
default = ufc_edge.data.ufcstats.settings
